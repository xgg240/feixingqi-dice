# 飞行棋骰子工具 v2.6.3 — Win 编译依赖
# v2.6.2 失败: mitmproxy 8.0.0 要 cryptography<37 + flask<2.1, 我手锁的 42/3.0 冲突
# v2.6.3: 只锁顶层, 子依赖全让 pip 自动解 (mitmproxy 8 是 2022-03 老包, 锁太死反装不上)
# mitmproxy 8.0.0 依赖元数据:
#   cryptography<37 and >=36, flask<2.1 and >=1.1.1, pyOpenSSL<23, requests>=2.9.1,
#   aioquic, tornado, brotli, h2, hpack, wsproto, zstandard, etc. — pip 自动解

mitmproxy==8.0.0          # 8.x 纯 Python, mode_specs.py 不引 mitmproxy_rs
pydivert==2.1.0           # WinDivert Python wrapper (抓包/重定向, 跟 Rust 扩展无关)
pycryptodomex==3.19.0     # 提速某些加密运算
certifi==2023.7.22        # CA bundle
requests>=2.28.0          # HTTP 客户端 (mitmproxy 8 要 >=2.9.1)
setuptools<81             # v2.6.4: mitmproxy 8 contrib 用 pkg_resources, setuptools 81+ 默认不带

# emu_cert.py 用的 cryptography — 别锁, 让 mitmproxy 8 拉它要的 36.x 系列
# (之前锁 41/42 全跟 mitmproxy 8 冲突, 锁 <37 才安全)

# v2.6.3 教训: v2.4.0 那 24 行手锁子依赖是 2024 年写的, 跟 mitmproxy 8 (2022) 装不上
# v2.6.3 只锁 5 个顶层, 业务依赖 (cryptography/aioquic/tornado/flask/pyOpenSSL) 全放开
# spec excludes 保留 mitmproxy_rs/mitmproxy_macos (8.x 没这些, 排着无害)
