# 飞行棋骰子工具 v2.4 — Win 编译依赖
# mitmproxy 升级到 11.x (兼容 cryptography 42 + flask 3 + pyOpenSSL 24 + Python 3.12)
# 旧 8.0.0 锁了一堆 2023 年初版本, 跟新 Python / 编译环境全冲突

mitmproxy==11.0.0         # mitmdump 子进程 + addon 框架 (原 8.0.0 升 11.x 兼容 Py3.12)
pycryptodomex==3.19.0     # cryptography 依赖
cryptography==42.0.0      # emu_cert 算证书 hash (mitmproxy 11.x 兼容)
certifi==2023.7.22        # CA bundle
requests==2.31.0          # HTTP 客户端

# mitmproxy 11.x 自动拉这些, 不用手动锁
# aioquic / pydivert / tornado / flask / werkzeug / bcrypt / argon2-cffi /
# zstandard / h2 / hpack / hyperframe / service-identity / pyOpenSSL /
# urllib3 / click / itsdangerous / markupsafe / wsproto / publicsuffix2 /
# sortedcontainers / pyparsing / pyasn1 / pyasn1-modules / ruamel.yaml /
# six / attrs / passlib / tldextract / pyelftools — pip 自动解决
