name: Build Windows EXE

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:  # 允许手动触发

permissions:
  contents: write   # 让 GITHUB_TOKEN 能创建 release (默认 read-only 不够)

jobs:
  build:
    runs-on: windows-latest
    defaults:
      run:
        shell: pwsh

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~\AppData\Local\pip\Cache
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
          restore-keys: ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller
          # 验证 mitmproxy 装好了 (spec 编译时依赖它)
          python -c "import mitmproxy; import mitmproxy.tools.main; import mitmproxy.dump; print(f'[verify] mitmproxy {mitmproxy.__version__} OK')"

      - name: PyInstaller build
        run: pyinstaller DiceTool.spec --clean

      - name: List output
        run: |
          Get-ChildItem dist/
          Get-Item dist\DiceTool.exe | Select-Object Name, Length

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: DiceTool-windows
          path: dist/DiceTool.exe
          if-no-files-found: error

      - name: Create Release (tag push only)
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: dist/DiceTool.exe
          generate_release_notes: true
